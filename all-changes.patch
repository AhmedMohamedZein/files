diff --git a/pom.xml b/pom.xml
index 0cc45a0..48e6b72 100644
--- a/pom.xml
+++ b/pom.xml
@@ -51,6 +51,11 @@
 				</exclusion>
 			</exclusions>
 		</dependency>
+		<dependency>
+			<groupId>com.opencsv</groupId>
+			<artifactId>opencsv</artifactId>
+			<version>5.9</version>
+		</dependency>
 		<dependency>
 			<groupId>org.apache.httpcomponents</groupId>
 			<artifactId>httpclient</artifactId>
@@ -69,7 +74,7 @@
 		<dependency>
 			<groupId>net.atos.mdi.commons</groupId>
 			<artifactId>commons</artifactId>
-			<version>v1.0.49-SNAPSHOT-0</version>
+			<version>v1.0.49-SNAPSHOT-5</version>
 		</dependency>
 		<dependency>
 			<groupId>org.apache.camel.springboot</groupId>
diff --git a/src/main/java/eg/com/mdi/batch/enums/FileTypeConfig.java b/src/main/java/eg/com/mdi/batch/enums/FileTypeConfig.java
index d72ab40..03b37b4 100644
--- a/src/main/java/eg/com/mdi/batch/enums/FileTypeConfig.java
+++ b/src/main/java/eg/com/mdi/batch/enums/FileTypeConfig.java
@@ -1,6 +1,6 @@
 package eg.com.mdi.batch.enums;
 
-import eg.com.mdi.batch.mapper.MdpFieldSetMapper;
+import eg.com.mdi.batch.mapper.ABSFieldSetMapper;
 import eg.com.mdi.batch.mapper.SkyBagFieldSetMapper;
 import lombok.Getter;
 import net.atos.mdi.commons.client.model.crm.CardDeliveryDetail;
@@ -9,7 +9,7 @@ import org.springframework.batch.item.file.mapping.FieldSetMapper;
 import java.util.Arrays;
 
 public enum FileTypeConfig {
-    MDP("MDP", new MdpFieldSetMapper()),
+    ABS("ABS", new ABSFieldSetMapper()),
     SKY_BAG("SKYBAG", new SkyBagFieldSetMapper());
 
     private final String type;
diff --git a/src/main/java/eg/com/mdi/batch/mapper/MdpFieldSetMapper.java b/src/main/java/eg/com/mdi/batch/mapper/ABSFieldSetMapper.java
similarity index 94%
rename from src/main/java/eg/com/mdi/batch/mapper/MdpFieldSetMapper.java
rename to src/main/java/eg/com/mdi/batch/mapper/ABSFieldSetMapper.java
index dee17cb..b360e56 100644
--- a/src/main/java/eg/com/mdi/batch/mapper/MdpFieldSetMapper.java
+++ b/src/main/java/eg/com/mdi/batch/mapper/ABSFieldSetMapper.java
@@ -4,7 +4,7 @@ import org.springframework.batch.item.file.transform.FieldSet;
 
 import java.util.Arrays;
 
-public class MdpFieldSetMapper extends AbstractCardDeliveryFieldSetMapper {
+public class ABSFieldSetMapper extends AbstractCardDeliveryFieldSetMapper {
     @Override
     protected boolean shouldSkip(FieldSet fieldSet) {
         return Arrays.stream(fieldSet.getValues()).allMatch(String::isBlank);
diff --git a/src/main/java/eg/com/mdi/batch/reader/FileReader.java b/src/main/java/eg/com/mdi/batch/reader/FileReader.java
index c150be9..70e314a 100644
--- a/src/main/java/eg/com/mdi/batch/reader/FileReader.java
+++ b/src/main/java/eg/com/mdi/batch/reader/FileReader.java
@@ -1,19 +1,34 @@
 package eg.com.mdi.batch.reader;
 
+import com.opencsv.CSVReader;
+import com.opencsv.CSVReaderBuilder;
 import eg.com.mdi.batch.enums.FileTypeConfig;
 import net.atos.mdi.commons.client.model.crm.CardDeliveryDetail;
+import org.jetbrains.annotations.NotNull;
+import org.slf4j.Logger;
+import org.slf4j.LoggerFactory;
 import org.springframework.batch.core.configuration.annotation.StepScope;
+import org.springframework.batch.item.ExecutionContext;
+import org.springframework.batch.item.ItemStreamException;
 import org.springframework.batch.item.ItemStreamReader;
-import org.springframework.batch.item.file.builder.FlatFileItemReaderBuilder;
-import org.springframework.batch.item.file.mapping.DefaultLineMapper;
-import org.springframework.batch.item.file.transform.DelimitedLineTokenizer;
+import org.springframework.batch.item.file.transform.DefaultFieldSet;
+import org.springframework.batch.item.support.AbstractItemStreamItemReader;
 import org.springframework.beans.factory.annotation.Value;
 import org.springframework.context.annotation.Bean;
-import org.springframework.core.io.FileSystemResource;
 import org.springframework.stereotype.Component;
 
+import java.io.FileInputStream;
+import java.io.InputStreamReader;
+import java.io.Reader;
+import java.nio.charset.StandardCharsets;
+import java.util.Arrays;
+import java.util.stream.Collectors;
+
 @Component
 public class FileReader {
+
+    private static final Logger log = LoggerFactory.getLogger(FileReader.class);
+
     @Bean
     @StepScope
     public ItemStreamReader<CardDeliveryDetail> partitionedFileReader(
@@ -21,24 +36,62 @@ public class FileReader {
             @Value("#{jobParameters['fileType']}") String fileType,
             @Value("#{stepExecutionContext['startLine']}") Long startLine,
             @Value("#{stepExecutionContext['endLine']}") Long endLine) {
+
         FileTypeConfig fileTypeConfig = FileTypeConfig.fromType(fileType);
-        DelimitedLineTokenizer tokenizer = new DelimitedLineTokenizer();
-        tokenizer.setDelimiter(",");
-        tokenizer.setStrict(false);
-        DefaultLineMapper<CardDeliveryDetail> lineMapper = new DefaultLineMapper<>();
-        lineMapper.setLineTokenizer(tokenizer);
-        lineMapper.setFieldSetMapper(fileTypeConfig.getMapper());
-
-        int linesToSkip = 1 + startLine.intValue();
-        int maxItemCount = (int) (endLine - startLine);
-
-        return new FlatFileItemReaderBuilder<CardDeliveryDetail>()
-                .name("csv-file-reader")
-                .resource(new FileSystemResource(filePath))
-                .encoding("UTF-8")
-                .linesToSkip(linesToSkip)
-                .maxItemCount(maxItemCount)
-                .lineMapper(lineMapper)
-                .build();
+
+        return new AbstractItemStreamItemReader<>() {
+            private CSVReader csvReader;
+            private long count = 0;
+            private final long maxCount = endLine - startLine;
+
+            @Override
+            public void open(@NotNull ExecutionContext executionContext) {
+                try {
+                    Reader reader = new InputStreamReader(new FileInputStream(filePath), StandardCharsets.UTF_8);
+                    csvReader = new CSVReaderBuilder(reader).withSkipLines(1 + startLine.intValue()).build();
+                } catch (Exception e) {
+                    throw new ItemStreamException("Failed to open file: " + filePath, e);
+                }
+            }
+
+            @Override
+            public CardDeliveryDetail read() throws Exception {
+                if (count >= maxCount) return null;
+                String[] line;
+                while ((line = csvReader.readNext()) != null) {
+                    String[] cleaned = Arrays.stream(line)
+                            .map(cell -> {
+                                if (cell == null) return "";
+                                String collapsed = Arrays.stream(cell.split("[\\r\\n]+"))
+                                        .map(String::strip)
+                                        .filter(part -> !part.isBlank())
+                                        .collect(Collectors.joining(" "));
+                                if (!cell.strip().equals(collapsed)) {
+                                    log.warn("Cleaned noisy cell: [{}] -> [{}]",
+                                            cell.replace("\n", "\\n"), collapsed);
+                                }
+                                return collapsed;
+                            })
+                            .toArray(String[]::new);
+                    boolean hasData = Arrays.stream(cleaned).anyMatch(cell -> !cell.isBlank());
+                    if (!hasData) {
+                        log.warn("Skipping blank row at partition offset {}", count);
+                        continue;
+                    }
+                    count++;
+                    return fileTypeConfig.getMapper().mapFieldSet(new DefaultFieldSet(cleaned));
+                }
+                return null;
+            }
+
+            @Override
+            public void close() {
+                try {
+                    if (csvReader != null) csvReader.close();
+                } catch (Exception e) {
+                    log.error("Failed to close CSV reader", e);
+                }
+            }
+        };
     }
 }
\ No newline at end of file
diff --git a/src/test/java/eg/com/mdi/batch/mapper/MdpFieldSetMapperTest.java b/src/test/java/eg/com/mdi/batch/mapper/ABSFieldSetMapperTest.java
similarity index 94%
rename from src/test/java/eg/com/mdi/batch/mapper/MdpFieldSetMapperTest.java
rename to src/test/java/eg/com/mdi/batch/mapper/ABSFieldSetMapperTest.java
index 8117660..760774e 100644
--- a/src/test/java/eg/com/mdi/batch/mapper/MdpFieldSetMapperTest.java
+++ b/src/test/java/eg/com/mdi/batch/mapper/ABSFieldSetMapperTest.java
@@ -7,9 +7,9 @@ import org.junit.jupiter.api.Test;
 import org.springframework.batch.item.file.transform.DefaultFieldSet;
 import org.springframework.batch.item.file.transform.FieldSet;
 
-class MdpFieldSetMapperTest {
+class ABSFieldSetMapperTest {
 
-    private final MdpFieldSetMapper mapper = new MdpFieldSetMapper();
+    private final ABSFieldSetMapper mapper = new ABSFieldSetMapper();
 
     @Test
     void shouldReturnNullWhenAllFieldsBlank() {
diff --git a/src/test/java/eg/com/mdi/routes/processors/CardDeliveryFileValidationProcessorTest.java b/src/test/java/eg/com/mdi/routes/processors/CardDeliveryFileValidationProcessorTest.java
index 04b00e5..8949f7d 100644
--- a/src/test/java/eg/com/mdi/routes/processors/CardDeliveryFileValidationProcessorTest.java
+++ b/src/test/java/eg/com/mdi/routes/processors/CardDeliveryFileValidationProcessorTest.java
@@ -35,14 +35,14 @@ class CardDeliveryFileValidationProcessorTest {
 
     @Test
     void shouldSetFileNameAndTypeForValidCsv() {
-        when(inMessage.getHeader(Exchange.FILE_NAME, String.class)).thenReturn("MDP_test-file.csv");
+        when(inMessage.getHeader(Exchange.FILE_NAME, String.class)).thenReturn("ABS_test-file.csv");
         try (MockedStatic<FileOperationUtils> utilities = mockStatic(FileOperationUtils.class)) {
-            utilities.when(() -> FileOperationUtils.extractFileExtension("MDP_test-file.csv")).thenReturn("csv");
-            utilities.when(() -> FileOperationUtils.extractFileName("MDP_test-file.csv")).thenReturn("MDP_test-file");
+            utilities.when(() -> FileOperationUtils.extractFileExtension("ABS_test-file.csv")).thenReturn("csv");
+            utilities.when(() -> FileOperationUtils.extractFileName("ABS_test-file.csv")).thenReturn("ABS_test-file");
             processor.process(exchange);
         }
-        verify(exchange).setProperty(Constants.FILE_NAME, "MDP_test-file");
-        verify(exchange).setProperty(Constants.FILE_TYPE, FileTypeConfig.MDP.name());
+        verify(exchange).setProperty(Constants.FILE_NAME, "ABS_test-file");
+        verify(exchange).setProperty(Constants.FILE_TYPE, FileTypeConfig.ABS.name());
         verify(idempotencyHandler, never()).handleCorruptedFile(any());
     }
 
diff --git a/src/test/java/eg/com/mdi/service/CardDeliveryBatchingServiceTest.java b/src/test/java/eg/com/mdi/service/CardDeliveryBatchingServiceTest.java
index 9825f42..ff9997b 100644
--- a/src/test/java/eg/com/mdi/service/CardDeliveryBatchingServiceTest.java
+++ b/src/test/java/eg/com/mdi/service/CardDeliveryBatchingServiceTest.java
@@ -76,15 +76,15 @@ class CardDeliveryBatchingServiceTest {
 
     @Test
     void testExecute_WithExistingJobParameters_ShouldReuseParameters() throws Exception {
-        String fileName = "MDP_02-03-2026.csv";
+        String fileName = "ABS_02-03-2026.csv";
         exchange.setProperty(Constants.FILE_NAME, fileName);
         exchange.setProperty(Constants.TEMP_FILE_PATH, "/tmp/test.csv");
-        exchange.setProperty(Constants.FILE_TYPE, "MDP");
+        exchange.setProperty(Constants.FILE_TYPE, "ABS");
         exchange.setProperty(net.atos.mdi.commons.constants.Constants.CORRELATION_ID_LOG_VAR_NAME, "corr-id");
         Map<String, Object> existingParams = new HashMap<>();
         existingParams.put(Constants.FILE_NAME, fileName);
         existingParams.put(Constants.TEMP_FILE_PATH, "/old/path.csv");
-        existingParams.put(Constants.FILE_TYPE, "MDP");
+        existingParams.put(Constants.FILE_TYPE, "ABS");
         existingParams.put("correlationId", "old-correlation-id");
         when(cardDeliveryRepository.loadJobParametersByFileName(fileName)).thenReturn(Optional.of(existingParams));
         when(jobLauncher.run(eq(processFileJob), any(JobParameters.class))).thenReturn(jobExecution);
@@ -98,10 +98,10 @@ class CardDeliveryBatchingServiceTest {
 
     @Test
     void testExecute_WithFailedJobStatus_ShouldThrowException() throws Exception {
-        String fileName = "MDP_02-03-2026.csv";
+        String fileName = "ABS_02-03-2026.csv";
         exchange.setProperty(Constants.FILE_NAME, fileName);
         exchange.setProperty(Constants.TEMP_FILE_PATH, "/tmp/test.csv");
-        exchange.setProperty(Constants.FILE_TYPE, "MDP");
+        exchange.setProperty(Constants.FILE_TYPE, "ABS");
         exchange.setProperty(net.atos.mdi.commons.constants.Constants.CORRELATION_ID_LOG_VAR_NAME, "corr-id");
         when(cardDeliveryRepository.loadJobParametersByFileName(fileName)).thenReturn(Optional.empty());
         when(jobLauncher.run(eq(processFileJob), any(JobParameters.class))).thenReturn(jobExecution);
@@ -116,7 +116,7 @@ class CardDeliveryBatchingServiceTest {
         String fileName = "CORRUPTED_FILE.csv";
         exchange.setProperty(Constants.FILE_NAME, fileName);
         exchange.setProperty(Constants.TEMP_FILE_PATH, "/tmp/corrupted.csv");
-        exchange.setProperty(Constants.FILE_TYPE, "MDP");
+        exchange.setProperty(Constants.FILE_TYPE, "ABS");
         exchange.setProperty(net.atos.mdi.commons.constants.Constants.CORRELATION_ID_LOG_VAR_NAME, "corr-id");
         StepExecution stepExecution = mock(StepExecution.class);
         ExecutionContext executionContext = new ExecutionContext();
@@ -156,12 +156,12 @@ class CardDeliveryBatchingServiceTest {
         String fileName = "EXISTING_FILE.csv";
         exchange.setProperty(Constants.FILE_NAME, fileName);
         exchange.setProperty(Constants.TEMP_FILE_PATH, "/tmp/existing.csv");
-        exchange.setProperty(Constants.FILE_TYPE, "MDP");
+        exchange.setProperty(Constants.FILE_TYPE, "ABS");
         exchange.setProperty(net.atos.mdi.commons.constants.Constants.CORRELATION_ID_LOG_VAR_NAME, "corr-id");
         Map<String, Object> existingParams = new HashMap<>();
         existingParams.put(Constants.FILE_NAME, fileName);
         existingParams.put(Constants.TEMP_FILE_PATH, "/old/path.csv");
-        existingParams.put(Constants.FILE_TYPE, "MDP");
+        existingParams.put(Constants.FILE_TYPE, "ABS");
         existingParams.put("someIntegerParam", 12345);
         when(cardDeliveryRepository.loadJobParametersByFileName(fileName)).thenReturn(Optional.of(existingParams));
         JobParameters result = service.resolveJobParameters(exchange);
@@ -176,7 +176,7 @@ class CardDeliveryBatchingServiceTest {
         String fileName = "TEST_FILE.csv";
         exchange.setProperty(Constants.FILE_NAME, fileName);
         exchange.setProperty(Constants.TEMP_FILE_PATH, "/tmp/test.csv");
-        exchange.setProperty(Constants.FILE_TYPE, "MDP");
+        exchange.setProperty(Constants.FILE_TYPE, "ABS");
         exchange.setProperty(net.atos.mdi.commons.constants.Constants.CORRELATION_ID_LOG_VAR_NAME, "corr-id");
         when(cardDeliveryRepository.loadJobParametersByFileName(fileName)).thenReturn(Optional.empty());
         when(jobLauncher.run(eq(processFileJob), any(JobParameters.class))).thenThrow(new RuntimeException("Job launcher failed"));
